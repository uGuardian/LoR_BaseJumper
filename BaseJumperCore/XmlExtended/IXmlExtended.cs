using System.Xml.Serialization;
#pragma warning restore CS0618

namespace Mod.XmlExtended {
	public interface IXmlExtended {
		// static interface methods are not supported before C# 8, so this is just used as a logic marker.
		//static XmlAttributeOverrides GetAttributeOverrides();
	}
}
